{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hamid-cni.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hamid-cni.labels" . | nindent 4 }}
{{- end }}
